# Log Archive Tool

A simple CLI tool to archive logs with date and time.

## Usage

./log_archive.sh <log_directory>

Example:

./log_archive.sh /var/log

## Features

- Archives logs using tar.gz
- Adds timestamp to archive name
- Creates archive folder automatically
- Simple CLI tool
